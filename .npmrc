# Requires npm >=11.10.0 (see engines.node in package.json / .node-version);
# older npm silently ignores this. Refuses to install a dependency version
# published less than this many days ago, as a defense against supply-chain
# attacks that get pulled/patched shortly after publishing.
min-release-age=7